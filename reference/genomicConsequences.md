# Variant-consequence vocabulary (offline reference data).

Returns the High/Medium/Low severity consequence vocabulary bundled with
the adapter as a data.frame (\`severity\`, \`consequence\`). No session
or network required.

## Usage

``` r
genomicConsequences()
```

## Value

A data.frame with columns \`severity\` and \`consequence\`.
