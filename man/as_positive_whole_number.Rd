% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_coerce.R
\name{as_positive_whole_number}
\alias{as_positive_whole_number}
\title{Validate a single positive whole number.}
\usage{
as_positive_whole_number(value, arg, call = sys.call(-1L))
}
\arguments{
\item{value}{The value to validate.}

\item{arg}{The argument's name, for the error message.}

\item{call}{The call to report in the error, by default the caller's, so
the rejection names the wrapper the researcher invoked.}
}
\value{
`value` as an integer scalar.
}
\description{
Rejects `NA`, `NaN`, `Inf`, a fraction, anything below 1, anything above
`.Machine$integer.max`, non-numeric input, and any length other than one,
so `page = 1.7` is an error rather than a silent truncation to 1. Each
rejection names the argument and what arrived, and raises a
`picsureValidationError`, which is a `picsureError`.
}
\keyword{internal}
