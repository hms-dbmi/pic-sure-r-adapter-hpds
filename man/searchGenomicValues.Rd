% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{searchGenomicValues}
\alias{searchGenomicValues}
\title{Look up valid values for a genomic annotation key (authorized platforms).}
\usage{
searchGenomicValues(
  session,
  genomicConceptPath,
  query = "",
  page = 1,
  size = 100,
  ...
)
}
\arguments{
\item{session}{A session from [`connect()`][picsure::connect] on an
authorized platform.}

\item{genomicConceptPath}{The genomic key, e.g. `"Gene_with_variant"` or
`"Variant_consequence_calculated"`.}

\item{query}{Optional search term to narrow results (e.g. `"BRCA"`).}

\item{page}{1-based page number.}

\item{size}{Page size (values per call).}

\item{...}{Additional keyword arguments forwarded to the Python call.}
}
\value{
A data.frame with a single `value` column. (The Python adapter's
  pagination metadata lives on the DataFrame's `.attrs` and does not survive
  reticulate conversion; paginate via `page`/`size`.)
}
\description{
Returns one page of values for a genomic key (e.g. all genes with variants),
as a data.frame with a single `value` column. Raise `size` to pull more
per call, or step `page` to walk the full set.
}
